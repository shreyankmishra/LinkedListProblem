using System;

namespace LinkedList
{
    class Program
    {
        static void Main(string[] args)
        {
            LinkedLists list = new LinkedLists();
            list.Add(70);
            list.Add(30);
            list.Add(56);
            list.Display();
        }
    }
}
